# 11/09/2026

Pedido: criar skills básicas em `AI/SKILLS/` e organizar `claude.md`/`AGENTS.md`. Referência indicada pelo usuário: o mesmo padrão já implementado no projeto `ccmergulho` (monorepo em `Documents/WELDER/DEV/ccmergulho`).

O que mudou:

- Diagnóstico: `claude.md`, `AGENTS.MD` e `.github/copilot-instructions.md` apontavam para `docs/ai/CONTEXT.md`/`RULES.md`/`DECISIONS.md`/`TODO.md`, que nunca existiram neste checkout — o conteúdo real sempre esteve em `AI/` na raiz. `AI/SKILLS/{BACKEND,DATABASE,FRONTEND,TESTING}` estavam vazias.
- Corrigidas as referências nos três arquivos para `AI/CONTEXT.md`, `AI/TASKS.md` e `AI/SKILLS/`.
- `AGENTS.MD` e `claude.md` reescritos no padrão do `ccmergulho`: `AGENTS.md` cobre regras gerais (ordem de leitura, checklist de fim de tarefa, arquitetura, segurança, idioma); `claude.md` ficou enxuto, só com a regra de responder sempre em português e apontando para `AGENTS.md`.
- `AGENTS.MD` também corrigido num ponto factual: a regra antiga citava uma camada de "Repositories" que não existe neste backend (os Services usam `PrismaService` diretamente).
- `AI/README.MD` atualizado para refletir a estrutura real (`SESSIONS/` é pasta com um arquivo por dia, não um `SESSIONS.md` único; removida a menção a um `DECISIONS.md` que não existe).
- `AI/CONTEXT.md` corrigido em dois pontos que citavam `docs/AI/` (caminho errado) e descreviam o problema de referências quebradas acima — agora registra que foi corrigido.
- Populadas as 4 pastas de `AI/SKILLS/` com micro skills (um arquivo markdown por tema, com exemplo real do código + checklist), seguindo o mesmo formato do `ccmergulho`:
  - `BACKEND/`: estrutura de módulo, guards (Jwt/Rotina/Roles), acesso a dados via Prisma, validação de DTOs, uploads, subir a API localmente.
  - `DATABASE/`: migrations, convenção de enums/estados, identidade/permissões no schema, seeds, banco local.
  - `FRONTEND/`: organização de módulo, TanStack Query, cliente HTTP, Dialog/feedback, permissões nas rotas, rodar localmente.
  - `TESTING/`: Jest, Vitest, prioridades de cobertura, checklist de fim de tarefa.
- Criado `.claude/skills/SKILL.md` (skill nativo do Claude Code) para "rodar o projeto localmente", orquestrando as micro skills acima — mesmo padrão do `run-mergulho-connect` do `ccmergulho`.

Nada foi renomeado (mantive `AGENTS.MD`/`claude.md` com a grafia já existente) e nenhum código de aplicação foi tocado — só documentação em `AI/`, `AGENTS.MD`, `claude.md`, `.github/copilot-instructions.md` e o novo `.claude/skills/SKILL.md`.
