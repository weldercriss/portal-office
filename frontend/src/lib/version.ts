/**
 * Versão do app, mostrada no cabeçalho ao lado do botão de tema.
 * Ao publicar, atualize este valor junto com o campo `version` do package.json.
 * Formato: MAJOR.MINOR.PATCH.
 */
export const APP_VERSION = '2.0.2';
